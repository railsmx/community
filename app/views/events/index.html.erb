<%= display_notice %>

<%= display_create_event_link %>

<h3>Eventos recientes</h3>
<% @current_events.each do |event| %>
  <div id="event_<%= event.id %>">
    name:<%= event.name %> |
    <%= link_to t('.edit'), edit_event_path(event) %>
  </div>
<% end %>
<p>
<h3>Eventos pasados</h3>
<% @past_events.each do |event| %>
  <div id="event_<%= event.id %>">
    name:<%= event.name%>
  </div>
<% end %>

<!-- <div class="container">
  <%= link_to t('.add_event'), new_event_path %>

  <div class="hero-unit hidden-phone">
    <h1>Eventos</h1>
    <p>Aqui nuestros Proximos Eventos</p>
    <p></p>
  </div>
  
  <div class="row main-features">
    <% @current_events.each do |event| %>
    <div id="event_<%= event.id %>">
      <div class="well">
        <h3><%= event.name %></h3>
        <h4>Organizador:</h4>
        <h5><%= event.organizer %></h5>
        <h4>Lugar:</h4>
        <h5><%= event.location %></h5>
        <h4>Fecha:</h4>
        <h5><%= event.date %></h5>
        <h4>Horar:</h4>
        <h5><%= event.time %></h5>
        <h4>Contacto:</h4>
        <h5><%= event.contact %></h5>
        <h4>Descripciòn:</h4>
        <p><%= event.description %></p>
      </div>
      <%= link_to t('.edit'), edit_event_path(event) %>
    </div>
    <% end %>  
  </div>

  <div class="hero-unit hidden-phone">
    <h1>Eventos Pasados</h1>
    <p>Aqui nuestros Eventos Pasados</p>
    <p></p>
  </div>

  <% @past_events.each do |event| %>
    <div class="row main-features">
      <div id="past_event_<%= event.id %>">
        <div class="well">
          <h3><%= event.name %></h3>
          <h4>Organizador:</h4>
          <h5><%= event.organizer %></h5>
          <h4>Lugar:</h4>
          <h5><%= event.location %></h5>
          <h4>Fecha:</h4>
          <h5><%= event.date %></h5>
          <h4>Horar:</h4>
          <h5><%= event.time %></h5>
          <h4>Contacto:</h4>
          <h5><%= event.contact %></h5>
          <h4>Descripciòn:</h4>
          <p><%= event.description %></p>
        </div>
        <%= link_to t('.edit'), edit_event_path(event) %>
      </div>
    <% end %>  
    </div>
   </div>
</div> -->






