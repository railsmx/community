<% title @event.name %>
<div class="content edit-event">
  <section class="left-section">
    <%= render partial: 'shared/sidebar', locals: { current_events: @current_events, current_posts: @current_posts } %>
  </section>

  <section class="right-section">
    <%= display_alert %>
    <h1><%= t('.edit_event_title') %> </h1>
    <p>
      Por favor llena la información de forma para resgistrar tu evento. Mientras más detallada sea la información proporcionada
      mayor interés se podrá generar para los posibles asistentes.
    </p><br>
    <%= render partial: 'form', locals: { event: @event, submit: t('.update_event') } %>
  </section>
</div>